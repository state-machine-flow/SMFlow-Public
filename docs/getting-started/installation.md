# Installation

> **Draft.** This page is outlined but not yet written. Track it in
> [issues](https://github.com/state-machine-flow/SMFlow-Public/issues).

- Download the installer for Windows, macOS, or Linux from [Releases](https://github.com/state-machine-flow/SMFlow-Public/releases)
- What the installer includes (editor, `smflow` CLI, simulator)
- Host toolchain prerequisites per target (g++/clang for Linux targets, arm-none-eabi / Pico SDK, ESP-IDF, avr-gcc)
- Verifying the install: `smflow targets`
