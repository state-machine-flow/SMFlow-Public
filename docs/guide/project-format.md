# Project file format

> **Draft.** This page is outlined but not yet written. Track it in
> [issues](https://github.com/state-machine-flow/SMFlow-Public/issues).

What lives in `project.smflow` (nodes, connections, tasks, `formatVersion`) and `project.iomap` (resource-to-pin bindings), how versions migrate, and why the file format is deliberately not the compiler's IR.
